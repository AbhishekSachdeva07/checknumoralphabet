#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main()
{
    char str;
    scanf("%c",&str);
    if (isalpha(str)){
        printf("alphabet");
    }
    else{
        printf("num");
    }
        
    return 0;
}
